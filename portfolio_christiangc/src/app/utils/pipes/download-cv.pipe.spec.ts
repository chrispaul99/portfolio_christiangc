import { DownloadCVPipe } from './download-cv.pipe';

describe('DownloadCVPipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadCVPipe();
    expect(pipe).toBeTruthy();
  });
});
