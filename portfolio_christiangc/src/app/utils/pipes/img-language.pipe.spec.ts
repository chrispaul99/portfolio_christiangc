import { ImgLanguagePipe } from './img-language.pipe';

describe('ImgLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ImgLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
